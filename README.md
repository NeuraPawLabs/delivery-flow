# Delivery Flow

`delivery-flow` bootstraps the main-agent workflow controller described in the
shared controller spec.

Current scope in `LC16`:

- project scaffold
- controller state skeleton
- explicit backend mode selection
- owner-visible mode banner

Follow-up tasks add the `superpowers-backed` driver, fallback driver, and
dual-mode consistency verification.
